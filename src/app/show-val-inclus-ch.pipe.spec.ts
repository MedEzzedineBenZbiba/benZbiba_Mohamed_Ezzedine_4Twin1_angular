import { ShowValInclusChPipe } from './show-val-inclus-ch.pipe';

describe('ShowValInclusChPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowValInclusChPipe();
    expect(pipe).toBeTruthy();
  });
});
